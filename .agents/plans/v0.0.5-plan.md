# LazyDex v0.0.5-plan — Non-Functional & Broken UI Fixes

> **Tagline**: Polishing non-functional controls, broken UI stubs, and accessibility gaps into fully working features.
> **Origin**: Comprehensive UI audit identifying 24 HIGH, MEDIUM, and LOW severity issues across Dex, FilterSheet, Settings, Statistics, AddEdit, Browser, and Navigation.
> **Prerequisite**: Built on top of v0.0.2 / v0.0.3 / v0.0.4 after purging dead components (`LibraryBottomSheet.kt`, `DetailHeader.kt`, `DarkAccent`).

---

## 1. Summary of Issues to Resolve (24 Items)

| Severity | Issue # | Component / File | Problem Description |
|---|---|---|---|
| **HIGH** | #1 | `DexScreen.kt` | Search button click handler is a dead stub (`{ /* Search */ }`) |
| **HIGH** | #2 | `DexScreen.kt` | Grid/List toggle state (`isGridView`) exists but has no UI toggle button |
| **HIGH** | #3 | `FilterSheet.kt` | `RangeSlider` imported & parameters accepted, but rating range filter UI never renders |
| **HIGH** | #4 | `DexViewModel.kt` | `dateRangeStart`/`dateRangeEnd` filters exist in state/ViewModel but have no UI controls |
| **HIGH** | #5 | `DexViewModel.kt` | No search query state or title search filtering method in `DexViewModel` |
| **HIGH** | #6 | `AboutScreen.kt` | "What's coming (soon)" and "Help translate" links are empty stub lambdas |
| **HIGH** | #7 | `AppearanceScreen.kt` | Theme preset carousel is purely decorative (no click handlers, hardcoded checkmark) |
| **HIGH** | #8 | `LibraryBottomSheet.kt` | Purged (266-line dead component) |
| **HIGH** | #9 | `DetailHeader.kt` | Purged (122-line dead component) |
| **MEDIUM** | #10 | `StatisticsScreen.kt` | Movie & TV categories missing from Statistics breakdown |
| **MEDIUM** | #11 | `StatisticsScreen.kt` | `inProgressCount` in `MediaStats` is never displayed |
| **MEDIUM** | #12 | `StatisticsViewModel.kt` | Null initial state causes flash of zeros with no loading indicator |
| **MEDIUM** | #13 | `Color.kt` | Purged unused `DarkAccent` color definition |
| **MEDIUM** | #14 | `FilterSheet.kt` | Purged unused `DisplaySettings` icon import |
| **MEDIUM** | #15 | `UnifiedAddEditScreen.kt` | HTML preview hardcodes `Color.WHITE` (illegible in light mode) |
| **MEDIUM** | #16 | `AboutScreen.kt` | Discord link points to generic `discord.gg` homepage without server invite code |
| **MEDIUM** | #17 | `Theme.kt` | `dynamicColor=true` default with no user toggle control |
| **MEDIUM** | #18 | `AppearanceScreen.kt` | `coverTheming` switch exists in settings but theme never applies it |
| **LOW** | #19 | `AppearanceScreen.kt` | Uses deprecated `Divider` instead of `HorizontalDivider` |
| **LOW** | #20 | `UnifiedAddEditScreen.kt` | Uses platform legacy `DatePickerDialog` instead of Material3 `DatePicker` |
| **LOW** | #21 | `UnifiedAddEditScreen.kt` | `extractAverageColor` decodes full bitmap inefficiently (no `inSampleSize`) |
| **LOW** | #22 | `UnifiedAddEditScreen.kt` | Date picker clickable overlay on top of disabled `TextField` (accessibility issue) |
| **LOW** | #23 | `BrowserViewModel.kt` | Browser search has no debounce or pagination |
| **LOW** | #24 | `MainShellScreen.kt` | Hardcoded Unicode `▾` inside string template in bottom nav label |
| **LOW** | #25 | `AboutScreen.kt` | Contradictory text `Stable v0.0.1-beta` (fixed to `Version 0.0.1-beta`) |

---

## 2. Implementation Plan by Git Commits

### Commit 1: `fix(dex): implement search query filtering in DexViewModel and wire top bar search`
**Target Files:**
- [MODIFY] `app/src/main/java/app/lazydex/ui/dex/DexViewModel.kt`
- [MODIFY] `app/src/main/java/app/lazydex/ui/dex/DexScreen.kt`

**Tasks:**
- Add `searchQuery` `StateFlow<String>` to `DexViewModel` and `DexUiState`.
- Update `sortItems`/filter flow in `DexViewModel` to filter items matching `searchQuery` against title, alt titles, or author.
- Replace empty `{ /* Search */ }` onClick in `DexScreen.kt` top bar with a search text field toggle / active search bar overlay.

---

### Commit 2: `fix(dex): add grid/list view toggle action to DexScreen top bar`
**Target Files:**
- [MODIFY] `app/src/main/java/app/lazydex/ui/dex/DexScreen.kt`

**Tasks:**
- Add top bar action button with `Icons.Default.GridView` / `Icons.Default.ViewList` icon.
- Toggle `isGridView` state when user clicks the toggle action.

---

### Commit 3: `fix(filter): render rating range slider and wire date range pickers in FilterSheet`
**Target Files:**
- [MODIFY] `app/src/main/java/app/lazydex/ui/components/FilterSheet.kt`
- [MODIFY] `app/src/main/java/app/lazydex/ui/dex/DexScreen.kt`

**Tasks:**
- Implement `RangeSlider` UI section in `FilterSheet.kt` for `minRating`..`maxRating` (0.0 to 10.0 range).
- Add date range filter UI section (Start Date and End Date pickers) in `FilterSheet.kt` and wire `dateRangeStart`/`dateRangeEnd` to `DexViewModel.setDateRange(...)`.

---

### Commit 4: `fix(settings): implement AboutScreen placeholder actions and valid Discord link`
**Target Files:**
- [MODIFY] `app/src/main/java/app/lazydex/ui/settings/AboutScreen.kt`

**Tasks:**
- Implement dialogue/toast info for "What's coming (soon)" and "Help translate" placeholders.
- Update Discord URL to point to a valid invite link or fallback lazy2dev community URL.

---

### Commit 5: `fix(settings): implement theme preset selection, cover color theme, and dynamic color toggle`
**Target Files:**
- [MODIFY] `app/src/main/java/app/lazydex/ui/settings/AppearanceScreen.kt`
- [MODIFY] `app/src/main/java/app/lazydex/ui/theme/Theme.kt`
- [MODIFY] `app/src/main/java/app/lazydex/data/local/ThemePreferences.kt`
- [MODIFY] `app/src/main/java/app/lazydex/MainActivity.kt`

**Tasks:**
- Make theme presets in `AppearanceScreen` clickable and persist selected preset.
- Add user toggle switch for `dynamicColor` in `AppearanceScreen` and respect setting in `MainActivity` & `LazyDexTheme`.
- Apply `coverTheming` preference in detail/preview screens to extract and apply cover accent colors.
- Replace deprecated `Divider` with Material3 `HorizontalDivider` in `AppearanceScreen.kt`.

---

### Commit 6: `fix(statistics): add Movie & TV breakdown, inProgressCount, and initial loading state`
**Target Files:**
- [MODIFY] `app/src/main/java/app/lazydex/ui/statistics/StatisticsScreen.kt`
- [MODIFY] `app/src/main/java/app/lazydex/ui/statistics/StatisticsViewModel.kt`

**Tasks:**
- Add Movie and TV category cells to Categories Breakdown grid in `StatisticsScreen.kt`.
- Display `inProgressCount` in Overview summary card.
- Add loading state handling (`CircularProgressIndicator`) in `StatisticsScreen` when `statsState` is null to prevent zero flash.

---

### Commit 7: `fix(addedit): fix HTML preview text color, platform DatePicker, OOM bitmap decode, and accessibility`
**Target Files:**
- [MODIFY] `app/src/main/java/app/lazydex/ui/addedit/UnifiedAddEditScreen.kt`

**Tasks:**
- Change hardcoded `Color.WHITE` in HTML preview `TextView` to dynamic theme text color (`MaterialTheme.colorScheme.onSurface`).
- Replace legacy `android.app.DatePickerDialog` with Material3 `DatePickerDialog`.
- Optimize `extractAverageColor` bitmap decoding using `BitmapFactory.Options` with `inSampleSize`.
- Remove invisible clickable overlay over disabled `OutlinedTextField` and use standard clickable date picker text field.

---

### Commit 8: `fix(browser): add search debounce and pagination to BrowserViewModel`
**Target Files:**
- [MODIFY] `app/src/main/java/app/lazydex/ui/browser/BrowserViewModel.kt`

**Tasks:**
- Add `debounce(300ms)` operator to search input handling.
- Add pagination support (`page` parameter) to `BrowserViewModel` search state for infinite scrolling / loading more results.

---

### Commit 9: `fix(navigation): replace hardcoded Unicode character in bottom nav label`
**Target Files:**
- [MODIFY] `app/src/main/java/app/lazydex/ui/navigation/MainShellScreen.kt`

**Tasks:**
- Replace hardcoded Unicode `▾` string template with Material icon indicator (`Icons.Default.ArrowDropDown`) or clean styled label.

---

## 3. Verification & Acceptance Criteria

- All unit tests pass: `./gradlew test`
- Clean build: `./gradlew assembleDebug`
- Zero dead/unused code warnings in Android Lint: `./gradlew lint`
